<button onclick="openProfileModal('${file.senderUid}', '${file.senderUsername}')" 
        class="text-blue-600 hover:text-blue-800 hover:underline font-medium focus:outline-none">
    @${file.senderUsername || 'pengguna'}
</button>
